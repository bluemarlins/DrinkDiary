# Task Log

Claude와 `agy`가 수행하는 태스크의 살아있는 백로그/로그. 상태는 `todo` / `in-progress` /
`review` / `done` / `blocked` 중 하나. 완료 시 관련 커밋 해시를 기록한다. Phase 정의는
`app/docs/specs/planner/prd.md` 참조.

## 야간 자율 작업 중단 시점 요약 (사용자 요청으로 저장 후 정지)

사용자가 취침 전 6개 항목(MVP 구현·테스트, 디자인 시스템/모션, 한/영 로컬라이제이션, Advance 기능
구상, 더미 데이터 DB 점검, 상품화 완성도)을 자율 진행하도록 요청 → 이후 "저장하고 정지" 요청으로
중단. 아래는 이 시점의 정확한 상태다(과장 없이 있는 그대로).

- **완료 + 검증됨**: 고급 인사이트 UseCase(P2-1/P2-2), CSV 내보내기 UseCase(P2-4/P2-5), Room DAO
  더미데이터 CRUD 테스트(Robolectric), Advance 기능 구상 문서.
- **완료했으나 최종 게이트 미확인**: 고급 인사이트 UI(P2-3, 커밋 `ffce97d`) — `ktlintCheck`와
  컴파일은 통과 확인했지만 `testDebugUnitTest`/`lint` 전체 통과는 빌드가 느려 확인 못 하고 중단.
  **다음 세션에서 가장 먼저 `.\gradlew.bat :app:testDebugUnitTest :app:lint` 재실행 필요.**
- **착수 안 함**: CSV 내보내기 UI/파일 저장 진입점(P2-6), 한/영 로컬라이제이션(약 123개 하드코딩
  한글 문자열, 14개 파일 — 범위만 조사함), 디자인 시스템 모션/인터랙션 고도화, Phase 2 통합
  회귀(P2-8).
- **중요 사고**: 여러 `agy` 호출을 동시(백그라운드 포함)에 띄웠다가 그 중 하나가 워킹 트리를
  마지막 커밋으로 되돌리는 부작용을 일으켜 미커밋 작업이 통째로 사라진 적 있음(대화 컨텍스트에서
  복구 완료). 상세: `orchestration/harness.md` 5절 사고 이력. 이후로는 agy 호출을 한 번에 하나씩만
  실행하고 검증 즉시 커밋하는 방식으로 전환함.
- **push 안 함**: 이 세션에서 만든 모든 커밋은 로컬에만 있다. 사용자 확인 후 push 필요.

## 제품 재정의 (2026-08-13)

기존 MVP 정의를 문제 정의부터 다시 세웠다. 순서: 문제 정의 → 리서치 → MVP 범위 → PRD → 원칙 → 아키텍처.

| 산출물 | 위치 | 담당 |
| --- | --- | --- |
| 문제 정의 | `../specs/planner/problem-definition.md` | Claude |
| 시장 리서치 | `../departments/researcher/market-research-2026-08.md` | agy(`gemini-3.6-flash-high`) 조사 + Claude 검수. conversation `2440f94f` 계열 |
| MVP 범위 / PRD | `../specs/planner/mvp-scope.md`, `prd.md` | Claude |
| 설계 원칙(토론) | `../specs/planner/design-principles.md` | Claude 입장 + agy(`gemini-3.1-pro-high`) 반론. conversation `027b83f4` |
| 아키텍처 | `../specs/developer/software-architecture.md` | Claude |

**문서 승격 (2026-08-13)**: 위 5건을 `departments/` → `specs/`로 승격. 동시에 충돌 문서를 정리했다.

- **폐기**: `specs/planner/product-plan.md`(구 비즈니스 모델), 구 `specs/developer/software-architecture.md`
- **보관 이관**: `usecase.md`, `ui-flow.md`, `database-design.md`, `navigation-flow-usecases.md`
  → 각 `departments/<부서>/*-legacy.md`. 구 MVP를 기술하며 현재 코드와는 일치하나 재정의 방향과 충돌
- **부분 개정**: `specs/designer/branding.md` — 이름·컬러·아이콘만 유효, 가치제안·스토어카피·기능스펙 폐기

**미결(사용자 결정 대기)**: 타깃 연령 하한, 과금 축(개수 vs 출력). 둘 다 브랜딩 개정 전 확정 필요.

**리서치에서 확인된 검증 실패**: agy가 댄 출처 "Atomin User Engagement Analysis, 2024"는 실재하지 않음.
이 사고로 `delegate-research` 스킬을 만들었다.

## 재정의 구현 (2026-08-14)

`software-architecture.md` 8절 구현 순서 1~4번 완료. 전 구간이 처음으로 연결됐다.

| 단계 | 상태 | 실기기 검증 |
| --- | --- | --- |
| 1. 도메인 + `ObserveTasteProfileUseCase` | done | 단위 테스트 |
| 2. Room 스키마 재정의 + Repository/DAO | done | DB 직접 조회 |
| 3. F2 취향 입력 UI | done | 탭 5회 확인 |
| 4. F1 기록 저장 | done | DB에 기록·답 저장 확인 |
| 5. F3 취향 요약 화면 | done | 기록 6건 → `SFRE` 판정 확인 |
| 6. F1 컬렉션(목록·필터·상세·삭제) | done | 삭제 후 유형이 되돌려지는 것까지 확인 |

**실기기에서만 드러난 결함 4건** — 문서로도, HTML 프로토타입으로도 잡히지 않았다.

| 결함 | 원인 |
| --- | --- |
| 진행 표시가 상태바 뒤로 숨음 | `enableEdgeToEdge` 상태에서 인셋 미적용 |
| 탭 7회(PRD 상한 5회 초과) | 기본 경로가 `Trait.of(Wine)` 6축을 물음 → `Trait.shared`로 제한 |
| F3가 산미·떫음에 "3개 더 필요"라고 표시 | 기본 입력 경로가 묻지 않는 축이라 영원히 판정 불가. 유형까지의 거리도 실제보다 멀어 보임 |
| 하단 탭 목록·검색이 눌러도 무반응 | 해당 화면 미구현 상태로 노출. `DDScreenScaffold`가 핸들러 없는 탭을 그리지 않도록 고침 |
| 하단 탭이 같은 말을 두 번 표시("홈" 위에 "대시보드") | 아이콘 에셋이 없어 아이콘 자리에 텍스트를 넣고 라벨도 함께 뒀다. 이름을 한 번만 보여주도록 고침 |
| 영어 로케일에서 한/영이 뒤섞임 | 새 화면은 한국어 하드코딩인데 `values-en`이 남아 있었다. 한국어 단일로 확정하고 삭제 |

**검증 방법 메모**
- Room은 데이터를 WAL에 남긴다. `.db`만 뽑으면 **빈 DB로 읽힌다** — `-wal`을 반드시 같이 가져온다.
- 기기에서 파일을 뽑을 때 PowerShell `>` 리다이렉션은 바이너리를 깨뜨린다. `cmd /c "... > file"`을 쓴다.
- Git Bash는 `adb`에 넘기는 `/sdcard/...`를 Windows 경로로 바꿔버린다. adb는 PowerShell에서 호출한다.
- 하드웨어 BACK 키로 화면을 벗어나면 앱 밖으로 나간다. 화면 전환은 앱 안의 "뒤로"를 쓴다.

## 취향 모델 재검토 (2026-08-14)

사용자 질문("4축으로 발베니·아드벡·조니워커를 구분할 수 있나")에서 시작해 감각 4축 스킴을 검증했다.

| 산출물 | 위치 | 담당 |
| --- | --- | --- |
| 4축 검증 (103종 배치 + 시뮬레이션) | `../departments/planner/axis-validation-2026-08.md` | Claude |
| 구조 속성 입력 설계 검토 | `../departments/planner/structural-attributes-2026-08.md` | agy(`gemini-3.6-flash-high`, Product Planner) 조사 + Claude 검증. conversation `4ff4f270-4a44-4047-9c1a-1eeff2d56860` |

**핵심 결과**: 감각 4축으로 유형이 성립하는 술은 103종 중 9.7%뿐이고, 16유형 중 3개만 쓰인다.
원인은 감각 강도 축이 전부 단봉(가운데 51~56%)인데 이진화했기 때문이다. 축을 늘리면 악화된다.
또한 현행 판정기는 **없는 취향을 지어내며, 기록이 쌓일수록 심해진다**(40잔 기준 61%).

**agy 검증에서 재발한 실패**: ① 출처 날조 3건(기관명 없는 "실태 조사") ② **국내 경쟁 앱 누락**
— 3개만 보고했으나 검색 한 번에 글렌·위스키키·위스키파크 등 6개 추가 확인. 2026-08 시장 리서치
때와 같은 양상이다. → `delegate-research` 스킬에 "국내 서비스는 항상 Claude가 교차 검색" 반영 필요.

**agy의 기여**: Claude가 카디널리티만 볼 때 agy는 **"라벨에서 읽히는가"**라는 선행 제약을 지적했다.
구세계 와인은 품종을 라벨에 적지 않고(우리 데이터로 와인의 62%), 캐스크는 법적 표기 의무가 아니다.
→ 취향 연결이 가장 강한 두 속성이 사용자가 입력할 수 없는 바로 그 둘이라는 결론.

### 사용자 확정 사항 (2026-08-14)

| 결정 | 내용 |
| --- | --- |
| 입력 척도 | `Low` / **`Mid`(보통이었어요)** / `High` 3선택. `Unsure` 폐기 |
| 판정 방식 | 양극단 평균 비교 → **축값(0/1/2)과 만족도의 상관**. `Mid`도 데이터 |
| 유형 체계 | 16유형 → **81유형**. 축당 3상태(High / **중립 X** / Low). 중립은 1급 결과 |
| 선택 태그 | 위스키 분류·피트, 와인 색, 도수 구간, 구/신대륙 (전부 선택 입력) |
| 태그 제외 | 캐스크·품종(라벨에서 못 읽음 → 내장 사전), 숙성년수(가격 지표) |
| 내장 사전 | 도입. 추천·판정에 쓰되 **기록마다 스펙 시트를 만들지 않는다** |
| F3 원칙 | "자신의 기록에서만" → **"자신의 만족도에서만. 술의 사실은 참조 가능, 남의 평가는 금지"** |

개정한 문서: `prd.md`(F2·F2-1 신설·F3), `software-architecture.md`(2-3·2-4·3·4·4-1·4-2 신설),
`branding.md`(4-5), `mvp-scope.md`, `design-principles.md`(쟁점 6), `taste-type-naming.md`.

### 구현 (2026-08-14, 커밋 `fce718b`)

명세 1~4번(입력 3옵션 · 상관 판정 · 중립 1급 · F3 화면)을 구현했다. 코드와 명세가 다시 맞는다.

에뮬레이터 검증 — **구 엔진을 걸려 넘어지게 설계한 입력**으로 확인했다.

```text
무게감·향의세기 = 만족도와 완전 동행
단맛           = 일부러 무상관 (High/Low/Mid를 만족도와 무관하게 배치)
여운           = 전부 '보통' (분산 0 → 상관 미정의)

결과: XFRX  "묵직하며 향이 진합니다. 단맛과 여운은 크게 가리지 않으세요."
```

구 엔진이라면 단맛에서 방향을 찾아냈을 자리다. 55개 테스트 통과.

### 선택 태그 (2026-08-14, 5번)

`F2-1` 선택 태그와 `ObserveTagPreferenceUseCase`를 구현했다. 태그는 "더 남기기" 안에 있어
**기본 경로의 5탭 예산에 들어가지 않는다.**

에뮬레이터 검증 — **감각 축이 아무 말도 못 하는 상황**을 일부러 만들었다.

```text
위스키 6잔, 감각 4축을 전부 '보통'으로 답함
피트 태그만 스모키함 3잔(5·5·4) / 스모키하지 않음 3잔(2·2·3)

유형        XXXX "고루 즐기는 취향"      ← 아무것도 지어내지 않음
라벨로 본 취향  스모키함 4.7점(3잔)
            스모키하지 않음 2.3점(3잔)  ← 태그만이 쓸모 있는 답을 냄
```

DB 대조: 기록 6건, 답 24건 전부 `Mid`, `record_tags`에 `Peat/Peated ×3 @4.67`,
`Peat/Unpeated ×3 @2.33`. 71개 테스트 통과.

**이것이 태그 경로를 만든 이유다.** 감각 축은 술을 구분하지 못하는데(103종 중 9.7%),
태그는 구분할 뿐 아니라 **결과를 매장에 가져갈 수 있다.**

### 첫 질문 세분화 (2026-08-14)

사용자 제안으로 와인 색·위스키 분류를 **선택 태그에서 기본 경로로** 올렸다.

```text
무엇을 마셨나요?
  와인    레드 / 화이트 / 스파클링 / 그 외
  위스키  싱글몰트 / 블렌디드 / 버번 / 잘 모름
```

**탭은 늘지 않았다** — 원래도 주종을 고르는 화면이었고 선택지만 세분화했다. 에뮬레이터에서
"탭 5번으로 끝났습니다" 확인. 승격된 태그는 선택 영역에서 빠져 두 번 묻지 않으며,
이 규칙은 테스트로 고정했다(`DrinkPickerTest`). DB에 `WineColor/Red` 저장 확인. 74개 테스트 통과.

### 태그 승격 — 첫 기록 직후에 한 번 묻기 (2026-08-14)

보류했던 "설정에서 승격" 대신, 사용자 제안으로 **첫 기록 직후 한 번 묻는** 방식을 채택했다.
제안은 "첫 기록 **전**"이었으나 한 단계 뒤로 옮겼다.

| | 첫 기록 전 | 나중에 설정 | **첫 기록 직후(채택)** |
| --- | --- | --- | --- |
| 온보딩 마찰 | **S1 위반** | 없음 | 없음 — 이미 가치를 받은 뒤 |
| 답할 근거 | 없음(피트가 뭔지 모름) | 있음 | 있음 — 흐름을 방금 겪음 |
| 데이터 균질성 | 완벽 | **깨짐**(20잔째 켜면 앞 19잔에 없음) | 1건만 다름 |
| 발견 | 확실 | **안 열림** | 확실 |

에뮬레이터 검증: 첫 저장 직후 프롬프트 등장 → 피트 선택 → 둘째 기록에서 피트가 "더 남기기"
**밖에** 노출, 도수·산지는 접힌 채 유지 → **프롬프트 재등장 없음**. DB에
`(2, Peat, Peated)` 저장 확인. 78개 테스트 통과.

기본값은 **아무것도 추가하지 않음**이며, 아무것도 안 고른 것도 답으로 처리해 다시 묻지 않는다.

**설정 화면은 만들지 않았다.** 따라서 프롬프트에 "나중에 설정에서 바꿀 수 있다"고 쓰지 않았다 —
없는 것을 약속하지 않는다. 백업·Pro·내보내기가 모일 때 함께 만들고 이 항목도 그때 넣는다.

### 내장 사전 (2026-08-14, 6번)

캐스크·품종을 앱이 미리 알게 했다. 사용자는 **이름만 적는다.**

**조사 범위를 좁혀서 위임했다.** 103종 목록은 Claude 것이고 agy에는 **두 필드만** 채우게 했다 —
100줄짜리 사실 데이터를 통째로 받으면 검증이 불가능하다.
(`gemini-3.6-flash-high`, Market Researcher, conversation `9af52ae1-100c-4b64-82a8-9c6bcd8126a6`)

검증 결과 — **이번에는 깨끗했다.**

| 항목 | 결과 |
| --- | --- |
| 지어낸 술 / 누락 | 0건 / 0건 |
| 허용 어휘 위반 | 0건 |
| `확인불가` | 1건(프로방스 로제) — **agy가 맞다.** 4개 스타일 그룹에 로제 자리가 없다 |
| 아메리칸 위스키 10종 | 전부 `새오크` — 법정 요건과 일치 |
| 셰리 5종 | 전부 실제 셰리 중심(맥캘란 셰리오크·글렌드로낙·글렌파클라스·아부나흐·하이랜드파크) |
| 아일라 ex-bourbon | 라프로익·아드벡10·탈리스커·쿨일라 모두 `버번` |

"확인불가를 허용한다"는 프롬프트 문구가 이번에도 작동했다. 로제는 사전에서 **뺐다** —
모르는 것을 넣으면 그때부터 사전을 믿을 수 없다.

**정직하게 남길 것**: `혼합`이 64종 중 35종(55%)이고 `셰리`는 5종뿐이다. 시장의 실제 분포이지만,
셰리 취향을 판정하려면 사용자가 셰리 중심 위스키를 3잔은 마셔야 한다.

**설계**: 기록에 사전 id를 저장하지 않고 **이름으로 조회 시점에 찾는다.** 사전을 고치면 과거 기록에도
바로 반영되고, 스키마도 늘지 않는다. 별칭이 입력의 부분 문자열이어야 하므로 "아드벡"만 적으면
매칭되지 않는다 — 어느 아드벡인지 모르는 채로 단정하지 않는다.

에뮬레이터 검증: **캐스크를 한 번도 입력하지 않고** 영문 이름만 6종 적었더니
`셰리 캐스크 4.7점(3잔) / 버번 캐스크 2.3점(3잔)`이 나왔다. 95개 테스트 통과.

**발견한 결함**: FAB가 마지막 줄을 가렸다. 아래 여백만으로는 안 되는데 — **내용이 화면보다 짧으면
스크롤이 아예 생기지 않아** 걷어낼 방법이 없다. `BoxWithConstraints`로 최소 높이를 화면+FAB로
잡아 항상 그만큼은 스크롤되게 했다.

**adb 메모**: `input text`는 **한글을 못 친다.** 한글 이름이 필요한 검증은 영문 별칭으로 우회하거나
별도 IME가 필요하다. 공백은 `%s`로 넘겨야 한 인자로 들어간다.

> [!NOTE]
> 이 변경 이전에 저장된 기록은 `Unsure` 문자열을 갖고 있어 더는 매핑되지 않고 답 단위로 버려진다.
> 출시 전이고 테스트 데이터뿐이라 수용한다(`harness.md` §1 스키마 재정의 예외).

### agy 로스터 3.7 전환 (2026-08-16, 사용자 지시)

**로스터가 4개 → 3개가 됐다.** 플래시 계열을 `gemini-3.5-flash-medium`/`gemini-3.6-flash-high` →
**`gemini-3.7-flash-medium`/`gemini-3.7-flash-high`**로 올리고, **`gemini-3.1-pro-high`를 제외**했다.
남은 셋은 3.7 두 단계 + 승급 티어 `claude-sonnet-4-6`.

`pro-high` 제외는 사용자 판단이다 — **이름만 `pro`일 뿐 세대가 낡아 3.7 플래시보다 사고력도
코드 구성 능력도 떨어진다.** Claude는 처음에 "3.7이 pro보다 낫다는 근거가 하나도 없다"며 남겨뒀는데,
그건 **근거가 없다는 뜻이지 pro가 낫다는 뜻이 아니었다.** 로스터에 남길 이유가 "아직 안 치워봐서"면
그건 이유가 아니다. "무거운 구현" 역할은 `gemini-3.7-flash-high`가 이어받는다.

**부수 효과 하나가 오히려 이득이다.** 모델 선택이 계열 고르기에서 **"얼마나 생각해야 하는 일인가"**
한 가지로 줄었고, 첫 승급이 모델 교체가 아니라 사고 예산 증액이 됐다 — `medium` 실패 + `high` 통과면
원인이 "생각 부족"으로 좁혀지고, **둘 다 실패하면 모델이 아니라 프롬프트나 태스크 분류를 의심해야
한다**는 신호가 된다.

전환 전 확인: `agy models`에 `gemini-3.7-flash-{high,medium,low}` 실재, `gemini-3.7-flash-high`
1회 호출 성공(4.1초, conversation `465534bf-25ba-419f-bb42-7d1692d41d1f`). **문서에 적는 모델 ID가
실제로 돌지 않으면 로스터가 아니라 함정이므로** 표를 고치기 전에 호출해 봤다.

고친 파일: `AGENTS.md`(로스터 표), `harness.md` §6-2(표·로스터 밖 목록·승급 경로),
`agy-playbook.md`(모델 목록·로스터 제약·템플릿 5곳), `persona-registry.artifact.md`(부서별 기본
모델), `software-architecture.md` §8, `.claude/skills/delegate-research/SKILL.md`.

**이력은 승계하지 않았다.** P0-4·P2-5·P1-7·P2-3의 게이트 통과는 3.5/3.6이 받은 성적이라
3.7 행의 "실사용 이력"은 비워 뒀다. 과거 기록(이 로그의 P2-2·P0-4 행, 각 부서 문서의 출처 표기)은
**당시 실제로 쓴 모델을 그대로 남긴다** — 재현 정보를 소급 수정하면 재현이 불가능해진다.
`gemini-3.1-pro-high`의 실사용 기록도 `harness.md` §6-2에 남겼다. 제외된 모델의 기록이지만
거기서 얻은 교훈(구현은 위임 가능, 검증은 아니다 / 미설치 라이브러리 도입 / 스크래치 쓰기)은
**모델이 아니라 agy의 성질**이라 3.7에도 그대로 적용된다.

미검증 하나: **`generate_image`가 3.7에서 되는지 모른다.** 3.6에서 확인했을 뿐이다. §6-2에
이미지 태스크 한정 예외를 두되, 되돌릴 때는 근거를 적도록 했다.

**검증 절차는 축소하지 않았다.** agy의 실패 양상은 모델 성능이 아니라 위임 구조에서 나온다 —
2026-08-15 사전 구축이 깨끗했던 것도 모델이 아니라 **범위를 두 필드로 좁힌 덕이다.**

### DB 재구성 — v3 (2026-08-16)

무결성을 **코드 관례에서 스키마로 옮겼다.** `trait_answers`에 `UNIQUE(recordId, trait)`,
`record_tags`에 `UNIQUE(recordId, category)`.

**왜 지금 중요한가**: 선호 판정이 답 행들의 상관계수라 **중복 한 줄이 계수를 왜곡하는데 에러는
안 난다.** v2까지는 DAO의 "지우고 다시 넣기" 관례만이 이를 막았고, 그 관례를 지나치는 저장 경로가
하나 생기면 끝이었다. 편집 화면을 붙이기 직전이라 그 경로가 늘어나는 시점이다.

**`fallbackToDestructiveMigration()` 제거.** 스키마 재정의가 끝나 예외를 닫았다. 폴백이 남아 있으면
마이그레이션을 빠뜨린 날 **앱은 정상 실행되고 기록만 사라진다** — 즉시 터지는 쪽이 낫다.
`harness.md` §1의 "출시 전 예외"와 `problem-definition.md` 7-1의 방침을 소진 처리했다.

**`@Insert(REPLACE)` → `@Upsert`**: REPLACE는 부모 행을 지웠다 넣으므로 CASCADE로 답·태그가
같이 날아간다. 지금은 바로 뒤에서 다시 넣어 결과가 같았지만 우연이다. 부수 효과로 `@Upsert`는
UPDATE 경로에서 **`-1`을 반환**하므로, 그 값을 recordId로 쓰지 않도록 명시해 뒀다.

**마이그레이션은 실제 데이터를 상대한다.** 유니크 인덱스 생성 전에 중복을 먼저 지운다.
"우리 코드는 중복을 안 만든다"는 전제가 될 수 없다 — 중복 한 줄이면 인덱스 생성이 실패하고
앱이 아예 켜지지 않는다.

검증:

| 방법 | 결과 |
| --- | --- |
| `MigrationTest` 3건 | 통과. **중복 제거를 빼면 `SQLiteConstraintException`으로 실패**하는 것까지 확인 — 테스트가 이유 있게 통과한다 |
| 에뮬레이터 실제 v2 DB | 기록 6·답 24·태그 6에 v3 덮어쓰기 → `user_version` 2→3, 데이터 전량 보존, 크래시 없음 |
| 화면 결과 | `셰리 4.7 / 버번 2.3`, 기록 6개 — 업그레이드 전과 동일 |
| 게이트 | 99개 테스트 · ktlint · lint · assembleDebug 통과 |

`room-testing`은 도입하지 않았다(harness.md §10 — 라이브러리 추가 금지). 대신 v2 스키마를 손으로
세우고 **앱이 실제로 타는 경로**(Room이 `user_version`을 보고 `onUpgrade`를 태우는 경로)를 그대로 탄다.

### 편집 · 설정 화면 (2026-08-16)

**편집(F1-1)**: 상세 → "고치기". 이름·만족도·다시 살까·사진·태그·선택 항목 + **취향 답**까지.

- **폼을 복제하지 않았다.** `RecordDetailStep`을 그대로 쓰고 취향 편집만 슬롯으로 끼웠다.
  복제하면 "이름·만족도 필수" 같은 규칙이 한쪽에만 남는다.
- **ViewModel은 나눴다.** 작성은 탭을 세는 마법사, 편집은 폼이다. 합치면 양쪽이 조건문투성이가 된다.
- **`recordedAtMillis`를 덮지 않는다.** 마신 시각이지 고친 시각이 아니다. 오타를 고쳤다고 그 술을
  오늘 마신 것이 되면 목록 순서가 뒤집힌다. 원본을 `copy`로 덮는 이유가 이것이다.
- **편집 화면이 안 보여주는 고유 축 답도 유지한다.** 저장이 답을 통째로 갈아끼우므로 여기서 빠지면
  사용자가 건드린 적 없는 값이 영영 사라진다.
- 저장 로직을 `DrinkRecord.applying` 순수 함수로 뺐다 — ViewModel 안에 두면 이 규칙들을 검증하려고
  코루틴 테스트 라이브러리를 새로 들여야 한다(harness.md §10).

**설정(F7)**: 기록할 때 물어볼 태그. 그것 하나뿐이다.

- **없는 기능을 자리만 잡아두지 않았다.** 백업·내보내기·Pro는 화면이 생길 때 들어온다.
  동작 안 하는 항목이 보이면 사용자는 앱이 고장난 줄 안다.
- 목록은 `promotableTags` 한 곳에서 온다 — 프롬프트와 갈라지면 **거기서 켠 것을 여기서 끌 수 없다.**
- 스위치는 누르는 즉시 저장한다. "저장" 버튼을 두면 안 누르고 나간다.
- **정직하게 적었다**: "지금 켜도 이미 저장한 기록에는 그 답이 없어요." 숨기면 사용자는
  "왜 아직 아무것도 안 나오지"에서 앱이 고장난 줄 안다.
- 이제 약속할 수 있게 됐으므로 첫 기록 직후 프롬프트 문구에 "설정에서 언제든 바꿀 수 있어요"를 넣었다.

에뮬레이터 검증 — **앱 데이터를 지우지 않고** 마이그레이션으로 살아남은 기록 위에서 그대로:

| 확인 | 결과 |
| --- | --- |
| 폼 프리필 | `Talisker 10` 이름·취향 4축이 기존 값으로 채워짐 |
| 저장 반영 | 만족도 `3 / 5` → `5 / 5`, 무게감 `보통` → `묵직함` |
| 마신 시각 | `2026.08.15` 그대로 — 편집일(08-16)로 덮이지 않음 |
| 기록 수 | 편집 전후 1건 그대로 — 새 기록이 생기지 않음 |
| 설정 → 기록 화면 | 피트를 켜니 기록 상세 단계에서 **'더 남기기' 밖**에 노출 |
| 설정 유지 | 나갔다 들어와도 켜진 상태 유지 |
| 게이트 | 105개 테스트 · ktlint · lint · assembleDebug 통과 |

### 디자인 시스템 점검 + 데이터 표현 벤치마킹 (2026-08-16)

F4 착수 전 점검. 산출물 둘 다 **초안**이다.

| 문서 | 담당 | 비고 |
| --- | --- | --- |
| `../departments/researcher/data-presentation-benchmark-2026-08.md` | agy 조사 + Claude 검증 | `gemini-3.7-flash-high`, conversation `7b47b48e-f0d0-419a-8a12-6df5630cff0c` |
| `../departments/designer/design-system-audit-2026-08.md` | Claude | 명세와 코드를 맞대는 일이라 검증이 곧 작업이다 — 위임하지 않음 |

**3.7 첫 실작업 결과 (harness.md §6-2 표에 반영할 것)**: `status: SUCCESS`, 117초,
출력 17k 토큰. **수치 날조 0건** — "출처 없으면 수치를 쓰지 마라"가 작동했고 `확인 불가`도 2건 나왔다.

**대신 실패 양상이 옮겨갔다.** UI 문자열과 개수를 지어냈다:

- Oura 크로노타입 "4종" → 실제 **6종** (오류)
- 왓챠피디아 성향 라벨 이름 3개 → 기능은 실재하나 그 이름들은 확인 불가
- 왓챠피디아 `"(3 / 15)"` 진행률 문구 → 확인 불가. **agy 제언 1의 근거 전체가 여기 걸려 있었다**

→ **다음 프롬프트에 넣을 규칙**: *"UI 문구를 큰따옴표로 인용하지 마라."* 지금 규칙은 수치만
막았고 agy는 안 막힌 쪽으로 빈칸을 채웠다. **모델 세대가 아니라 규칙의 구멍 문제다.**

부수 소득: Oura의 진짜 조건(90일 + 수면 30회)이 agy 버전보다 **엄격했다** — 사실이 우리에게 더 유리했다.

**Claude 반론**: agy 제언 3개 중 1개만 채택했다.

- **채택 — Vivino식 양극단 1차원 바** [검증됨]. 우리 4축이 이미 양극단이고 `DrinkLabels.poles()`에
  반대말 쌍이 이미 있다. 지금 F3는 "묵직함" 칩 하나로 끝나 **축 위 어디쯤인지를 못 보여준다.**
- **반대 — 별점 히스토그램**: agy가 §2에서 "사용자는 0~5건 구간에 오래 머문다"고 스스로 써놓고
  수백 편 기준의 Letterboxd 화면을 옮기자고 했다. 6건짜리 5칸 막대는 잡음이다. 게다가 그건
  취향이 아니라 **평점 습관**을 보여준다 — F3의 주제가 아니다.
- **반대 — 진행률 게이지**: 우리 판정은 스칼라가 아니다(축당 표본 6 **그리고** |r| ≥ 0.45).
  **게이지가 100%인데 유형이 안 나오면 지금보다 나쁘다.** 문장은 조건을 얼버무릴 수 있지만
  게이지는 못 한다.

**디자인 시스템 감사 — 확인된 사실**: 카탈로그 약 50개 중 **실존 6개**. 문서 상단의
2026-08-13 자기 진단("코드에 있으나 미문서화 10개")조차 **9개가 이미 삭제된 컴포넌트**라 오답을
가르치고 있다. 코드 결함은 넷: 만족도 포맷 3종 불일치, 간격 토큰 사용처 0곳(그리고 최다 사용값
20dp가 스케일에 없음), 차트 색 사용처 0곳, 컬렉션 목록에 재구매 여부 없음.

**M3 직접 사용은 결함으로 보지 않는다.** `DDPrimaryButton = Button` 래퍼 50개는 일관성이 아니라
이름만 늘린다. 이 저장소가 만든 진짜 공용 장치는 `DD*`가 아니라 **`DrinkLabels`**다.

## Phase 0 — 하네스/스캐폴딩 구축

| ID | 태스크 | 담당 | 상태 | 커밋 | 비고 |
| --- | --- | --- | --- | --- | --- |
| P0-1 | 문서 구조화 (product-plan/harness/agy-playbook/task-log) | Claude | done | (본 작업) | |
| P0-2 | ktlint + .editorconfig 도입 | Claude | done | (본 작업) | 기존 코드 위반 수정 완료, `ktlintCheck`/`testDebugUnitTest`/`lint` 모두 통과 확인 |
| P0-3 | CLAUDE.md에 Multi-agent workflow 섹션 추가 | Claude | done | (본 작업) | |
| P0-4 | 벤치마킹 앱 조사 (agy 파이프라인 첫 검증 호출) | agy | done | | 파일 수정 없이 완료, 결과를 product-plan.md 5절에 반영. 재현: `agy -p "..." --model gemini-3.5-flash-medium --output-format json`, conversation_id `2440f94f-c74c-4303-99b2-fdfa4d0d8cec` |

## Phase 1 — 상품화 준비 (코드 작성 없음, 문서/기획 산출물)

Billing 연동은 Phase 3(가장 마지막)으로 이동. Phase 1은 순수 기획/브랜딩/카피 작업이다.

| ID | 태스크 | 담당 | 상태 | 커밋 | 비고 |
| --- | --- | --- | --- | --- | --- |
| P1-1 | 앱 이름 후보 브레인스토밍 (대량 생성) | agy | done | | 1차 conversation `fe5823a5`(감성적이라 반려) → 2차 conversation `c6d24b88`(기능 직관성 기준 재생성). **사용자 확정: 테이스트 아카이브 / Taste Archive** — `app_name` 리소스 반영 완료 |
| P1-2 | 가치 제안(Value Proposition) 확정 | Claude | done | | `../specs/designer/branding.md` 2절 |
| P1-3 | 브랜딩 방향(톤앤매너, 컬러/키워드) 확정 | Claude | done | | `../specs/designer/branding.md` 3절, 기존 `ui/theme` 팔레트 그대로 채택 |
| P1-4 | Play 스토어 설명(짧은/긴 설명, 키워드) 초안 작성 | agy | done | | conversation `f2370ee9`. Claude 검토 완료, `../specs/designer/branding.md` 4절 |
| P1-5 | Free 기능 최종 스펙 확정 | Claude | done | | `../specs/designer/branding.md` 5절, 기존 MVP 범위 그대로 확정 |
| P1-6 | 프리미엄(Advance) 기능 최종 스펙 확정 | Claude | done | | `../specs/designer/branding.md` 6절, 우선순위: 고급 인사이트 > 내보내기 > 테마/위젯 |
| P1-7 | App Icon 컨셉 방향 제시 + 실제 이미지 생성 | agy | done | | 컨셉 conversation `e24a39f0` → **사용자 확정: 컨셉 2**. `generate_image`로 실제 PNG 생성(conversation `654b81c6`), Claude가 밀도별 리사이즈 후 앱 리소스 반영 완료 |
| P1-8 | Phase 1 결과 통합 (`../specs/designer/branding.md` 작성) + 사용자 마일스톤 보고 | Claude | done | | 이름/아이콘 모두 확정 및 반영 완료 — Phase 2 착수 승인 대기 |

## Phase 2 — MVP 고도화 구현

착수 전 도구 상태 확인(harness.md 6-1절 기준, 이 phase 착수 시점 1회 확인):
- **Skill**: `jetpack-compose-m3`는 이름과 달리 Wear OS 전용이라 이 앱(일반 모바일)에는 적용 불가.
  `styles`(Compose Styles API)는 현재 코드베이스가 쓰지 않는 별도 API 도입 결정이라 지금 범위에
  끌어오지 않음. `testing-setup`은 이미 확립된 유닛테스트 패턴(UseCase/ViewModel/Mapper)으로
  충분해 신규 테스트 하네스 구축은 불필요 — 즉, 이번 Phase의 Compose/테스트 작업에 skill이 주는
  이점은 없고 기존 `../specs/designer/design-system.md`/`harness.md` 컨벤션을 그대로 따르는 것으로 충분하다.
- **Android CLI/기기**: `adb devices`에 실기기(`R3CY50L9FCT`) 연결 확인됨 → UI 작업은 커밋 전
  루트 `installDebugApk` 태스크로 실기기에 설치해 시각 검증 가능(단순 컴파일/린트 통과에 그치지
  않음).
- **MCP**: 이 세션에 인증·연결된 MCP 중 Android/모바일 관련은 없음(Claude Browser는 웹 전용) →
  배분 판단에서 제외.

담당 배분(Claude: 설계/알고리즘/최종 리뷰+실기기 검증, agy: 명세에 따른 구현+테스트 작성):

| ID | 태스크 | 담당 | 상태 | 커밋 | 비고 |
| --- | --- | --- | --- | --- | --- |
| P2-1 | 고급 인사이트 UseCase 설계 (트렌드/가격대 만족도/재구매율 계산 알고리즘 확정) | Claude | done | c5dee4e | |
| P2-2 | 고급 인사이트 UseCase 구현 + 단위 테스트 | agy | done | c5dee4e | gemini-3.1-pro-high, `ktlintCheck`/`testDebugUnitTest`/`lint` 통과 확인 |
| P2-3 | 고급 인사이트 대시보드 UI (신규 카드/화면, DD* 컴포넌트 재사용) | agy+Claude | review | ffce97d | 데이터 유실 사고로 Claude가 대화 컨텍스트에서 재작성. `ktlintCheck`+컴파일 통과 확인, `testDebugUnitTest`/`lint` 최종 확인 필요. 기기 미연결로 실기기 시각 검증 못함 |
| P2-4 | 데이터 내보내기(CSV) UseCase + 파일 쓰기 로직 설계 | Claude | done | b6625f3 | |
| P2-5 | 데이터 내보내기 UseCase 구현 + 단위 테스트 | agy | done | b6625f3 | gemini-3.5-flash-medium, `ktlintCheck`/`testDebugUnitTest` 통과 확인 |
| P2-6 | 데이터 내보내기 UI (설정/내보내기 진입점, SAF 파일 저장) | agy | todo | | 착수 안 함 |
| P2-7 | 커스텀 테마/위젯 | — | todo | | 착수 안 함, 우선순위 최하 |
| P2-8 | Phase 2 통합 리뷰 + 전체 회귀 테스트 + 실기기 설치 검증 | Claude | todo | | 착수 안 함. P2-3 최종 게이트 확인부터 시작 |
| P2-11 | (계획 외 추가) 디자인 시스템 개정 및 모션/인터랙션 고도화 | agy | done | | `specs/designer/design-system.md` 전면 승격 및 앱 UI 디자인 시스템 적용 완료 (ktlint/unit test/lint 통과) |

## Phase 3 — Billing 연동 (최종 단계)

| ID | 태스크 | 담당 | 상태 | 커밋 | 비고 |
| --- | --- | --- | --- | --- | --- |
| P3-1 | Billing 연동 방식 결정 (Google Play Billing Library, entitlement 로컬 저장 전략) | Claude | todo | | 아키텍처 결정 — agy 위임 안 함 |
| P3-2 | Entitlement 게이팅 인프라 구현 | agy | todo | | P3-1 결정 이후 착수 |

## Phase 4

착수 시점에 세부 태스크로 분해해 이 문서에 추가한다.
